<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Wrong Password</title>
    <style>
      :root{
        --bg1: #0f172a;
        --bg2: #102040;
        --card: rgba(255,255,255,0.06);
        --accent: #ff5757;
        --accent-2: #ff9b9b;
        --glass: rgba(255,255,255,0.06);
      }

      *{box-sizing:border-box}
      html,body{
        height:100%;
        margin:0;
        font-family: Inter, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
        -webkit-font-smoothing:antialiased;
        -moz-osx-font-smoothing:grayscale;
        background: linear-gradient(135deg,var(--bg1), var(--bg2));
        color:#fff;
      }

      .wrap{
        min-height:100vh;
        display:grid;
        place-items:center;
        padding:2rem;
      }

      .card{
        width:100%;
        max-width:420px;
        background: linear-gradient(180deg, rgba(255,255,255,0.03), rgba(255,255,255,0.02));
        border:1px solid rgba(255,255,255,0.06);
        border-radius:14px;
        padding:28px;
        box-shadow: 0 8px 30px rgba(2,6,23,0.6);
        text-align:center;
        backdrop-filter: blur(6px);
      }

      .emoji{
        font-size:48px;
        display:inline-block;
        transform: translateY(-4px);
        filter: drop-shadow(0 6px 18px rgba(0,0,0,0.6));
      }

      h1{
        font-size:22px;
        margin:12px 0 6px;
        letter-spacing:0.2px;
      }

      p.lead{
        margin:0 0 20px;
        color:rgba(255,255,255,0.8);
        font-size:14px;
      }

      form{display:flex;flex-direction:column;gap:12px;align-items:center}

      button{
        appearance:none;
        -webkit-appearance:none;
        border:0;
        padding:10px 18px;
        font-size:15px;
        border-radius:10px;
        cursor:pointer;
        min-width:150px;
        background: linear-gradient(90deg,var(--accent), var(--accent-2));
        color:#fff;
        box-shadow: 0 8px 18px rgba(255,87,87,0.12), 0 2px 6px rgba(0,0,0,0.4) inset;
        transition: transform .12s ease, box-shadow .12s ease, opacity .12s ease;
      }

      button:hover{transform: translateY(-3px); box-shadow: 0 12px 30px rgba(255,87,87,0.14)}
      button:active{transform: translateY(-1px)}
      button:focus{outline:3px solid rgba(255,87,87,0.12)}

      /* small decoration */
      .line{
        height:8px;
        width:80px;
        border-radius:100px;
        background: linear-gradient(90deg, rgba(255,87,87,0.25), rgba(255,155,155,0.08));
        margin:14px auto 18px;
      }

      @media (max-width:480px){
        .card{padding:20px;border-radius:12px}
        h1{font-size:20px}
        .emoji{font-size:44px}
      }
    </style>
  </head>
  <body>
    <div class="wrap">
      <div class="card" role="alert" aria-live="polite">
        <div class="emoji">❌</div>
        <h1>Wrong Password</h1>
        <div class="line" aria-hidden="true"></div>
        <p class="lead">The password you entered doesn't match our records. Please try again.</p>

        <form method="get" action="/user">
          <button type="submit" aria-label="Try again">Try Again</button>
        </form>
      </div>
    </div>
  </body>
</html>
