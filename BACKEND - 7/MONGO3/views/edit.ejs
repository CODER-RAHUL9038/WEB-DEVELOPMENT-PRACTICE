<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Chat</title>
    <style>
      * {
        box-sizing: border-box;
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      }

      body {
        background-color: #e5ddd5;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
      }

      .edit-container {
        background-color: #ffffff;
        width: 400px;
        padding: 0;
        border-radius: 15px;
        box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
        overflow: hidden;
        animation: fadeIn 0.6s ease;
      }

      /* Header */
      .header {
        background: linear-gradient(135deg, #075e54, #128c7e);
        color: white;
        text-align: center;
        padding: 15px 10px;
        font-size: 18px;
        font-weight: 600;
        position: relative;
      }

      /* Back link */
      .back a {
        position: absolute;
        left: 15px;
        top: 14px;
        text-decoration: none;
        color: #fff;
        font-size: 15px;
        font-weight: 600;
        transition: opacity 0.3s ease;
      }

      .back a:hover {
        opacity: 0.8;
      }

      /* Form */
      form {
        display: flex;
        flex-direction: column;
        gap: 15px;
        padding: 25px;
      }

      textarea {
        width: 100%;
        height: 130px;
        padding: 10px;
        border: 1px solid #ccc;
        border-radius: 8px;
        font-size: 15px;
        resize: none;
        transition: all 0.3s ease;
      }

      textarea:focus {
        outline: none;
        border-color: #25d366;
        box-shadow: 0 0 6px rgba(37, 211, 102, 0.5);
      }

      button {
        background-color: #25d366;
        color: white;
        border: none;
        padding: 12px;
        border-radius: 8px;
        cursor: pointer;
        font-size: 15px;
        font-weight: 600;
        transition: all 0.3s ease;
      }
      input[type="text"],
      textarea {
        width: 100%;
        padding: 12px;
        margin: 8px 0;
        border: 1px solid #ccc;
        border-radius: 8px;
        font-size: 14px;
        box-sizing: border-box;
        transition: border-color 0.3s ease;
        outline: none;
      }

      input[type="text"]:focus,
      textarea:focus {
        border-color: #007bff;
      }

      button:hover {
        background-color: #20b858;
        transform: translateY(-1px);
      }

      /* Animation */
      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: translateY(10px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }
    </style>
  </head>

  <body>
    <div class="edit-container">
      <div class="header">
        <div class="back"><a href="/chats">⬅ Back</a></div>
        Edit Message ✏️
      </div>

      <form method="post" action="/chats/<%= chat._id %>?_method=PUT">
        <input
          type="text"
          name="from"
          placeholder="Edit Sender"
          value="<%= chat.from %>"
        />
        <input
          type="text"
          name="to"
          placeholder="Edit Recipient"
          value="<%= chat.to %>"
        />
        <textarea name="msg" id="msg" required><%= chat.msg %></textarea>
        <button type="submit">💾 Update Message</button>
      </form>
    </div>
  </body>
</html>
